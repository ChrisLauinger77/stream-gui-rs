# Phase 0 architecture

```text
React (developer screen, public DTOs only)
    ↓ typed Tauri IPC
Rust backend
    ├── Twitch device authorization / validation / refresh
    │      └── CredentialStore → process memory (Phase 0)
    ├── versioned settings
    └── Streamlink discovery / probe / session supervision
            ↓ native process + argument array, never a shell
        Streamlink
            ↓ Streamlink's default player discovery
        external player
```

## Ownership

`domain` holds errors, request DTOs and the service coordinator. `streamlink`, `twitch`, `config`, `credentials`, `diagnostics`, and `platform` have no Tauri dependency. `desktop` composes services, resolves the platform application directory, opens the window, and handles shutdown. `commands` contains only adapters. The optional `desktop` Cargo feature allows backend tests without WebView libraries.

The frontend has `app`, `components`, `features`, `lib`, and `styles`. React state contains only UI inputs and backend snapshots. There is no frontend OAuth client or token persistence. There are no placeholder stores, assets, player, or background modules; background auth and process tasks remain with the services that own their lifecycle.

## IPC

Rust DTOs derive `TS` and generate `src/lib/generated.ts`. A checked-in equality test enforces the contract. `src/lib/ipc.ts` is the only frontend `invoke` entry point and accepts a closed set of named operations and typed request objects. Rejections carry `{code, message}`. External provider bodies never become errors or logs.

The only commands are backend diagnostics, probe/save, launch, stop, session snapshots, auth status/login/logout/validate/refresh, and opening the current Twitch verification link. No frontend-selected shell, arbitrary argv, generic filesystem API or arbitrary browser URL is exposed. Selecting a custom Streamlink executable is an intentional local trust decision: version probing is validation, not executable sandboxing.

The main window has an explicit command allowlist generated by Tauri's application manifest. Remote origins get no capability. Production CSP blocks third-party scripts, direct web API connections, frames, and form submission. Only Rust contacts Twitch. The browser-opening command accepts no URL argument and checks the provider's HTTPS Twitch activation URL.

## Streamlink sessions

Discovery considers absolute PATH entries and rejects relative custom paths, directories, non-executable Unix files, and Windows script wrappers. The version probe drains both pipes concurrently with bounded capture and a five-second deadline. It kills and reaps on timeout. Launch re-probes the saved executable and uses a validated HTTPS Twitch channel URL and a restricted quality value.

The fixed argument prefix is `--no-config --loglevel info --`. Streamlink config files and the old GUI's credentials are never read by the application. Twitch API credentials are not Streamlink playback credentials and are not forwarded to it.

Each session has a unique UUID and one task owning its child. stdout and stderr are read concurrently. Process wait status, not log text such as `Starting player:`, controls `running → stopping → exited/failed`. Stop is idempotent for retained IDs, and always waits for reaping. Spawn failures create no session. Eight active sessions and sixteen retained snapshots bound history; old terminal snapshots may be evicted. An unknown/evicted ID returns `not_found`.

Per session, 200 log entries are retained. Individual lines are bounded to 2,048 input bytes; oversize lines are omitted while the pipe continues to drain. Invalid UTF-8 is replaced, terminal control characters are stripped, and common credential-related lines are suppressed. Logs are displayed as React text and never persisted. An untrusted custom executable can still output arbitrary sensitive content; this filter is not a universal secret detector.

Unix children start in a separate process group. Windows children are attached to a kill-on-close Job Object. Stop and normal parent exit clean up owned descendants, while the direct child is reaped. Readers are canceled if an inherited pipe stays open after cleanup. Intentionally detached external players are outside this prototype's ownership boundary. Windows job assignment happens just after spawn, so a descendant that escapes before assignment is a known prototype race; no suspended-process launcher is implemented.

Desktop exit first stops new launches, signals all owned sessions, and awaits cleanup. `kill_on_drop` and process group/job ownership are backups. An uncatchable Unix app kill cannot guarantee cleanup; this is not a daemon/process recovery implementation.

## Authentication and settings

One Rust service serializes device polling, validation, refresh, and logout. Bounded HTTP calls cannot complete behind logout and restore a cleared session. Device polling honors the server interval, increases it on slow-down, backs off temporary failures, expires locally, and is canceled by logout. Refresh rotation is serialized; new credentials replace old ones before validation so a transient validation failure does not reuse a consumed refresh token. An ambiguous refresh result requires login again. Startup and hourly validation are performed by a Rust task, independently of UI polling.

`CredentialStore` is a replaceable Rust-only interface. Phase 0 uses `MemoryCredentialStore`; the token type has neither a serialization nor a debug implementation, and owned token buffers zeroize on drop. An OS keychain implementation is deferred, as are persistent sessions and crash recovery for rotating tokens.

Settings are strict version 1 JSON containing only `streamlinkPath`. The directory comes from Tauri's `app_config_dir()`. Saves use a temporary file in the same directory and atomic replacement. Invalid/unsupported versions are rejected without automatic migration. A probe failure leaves the previous settings unchanged.
