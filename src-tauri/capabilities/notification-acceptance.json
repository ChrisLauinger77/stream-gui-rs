{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "notification-acceptance",
  "description": "Fixed native notification acceptance actions, opt-in debug builds only.",
  "windows": ["main"],
  "permissions": ["allow-dev-notification-test"]
}
